# Copy this file to .env.local and fill in your OpenRouter API key.
# Files named .env.local are git-ignored by default and safe for local secrets.

# Required for the chatbot (OpenRouter API)
VITE_OPENROUTER_API_KEY=sk-or-your_openrouter_key_here

# Notes:
# - Keys must start with "sk-or-" and have access to the selected model.
# - Restart the dev server after editing this file so Vite picks up the change.
